function checkIp(){
    var input = document.getElementById("btn-ip").value;
    var pattern = /^$/;
    // var temp = "IP chính xác";
    // var str;
    // for(var i = 0; i < input.length; i+=5)
    // {
    //     str = input.substr(i, 4);
    //     if(!pattern.exec(str))
    //     {
    //         temp = "IP không đúng định dạng: ";
    //         break;
    //     }
    // }
    // alert(temp);
}